{
  "name": "stock-heat-landing",
  "version": "1.0.0",
  "description": "热股 Stock Heat Tracker 推广页面（Cloudflare Pages 部署）",
  "private": true,
  "scripts": {
    "dev": "wrangler pages dev .",
    "deploy": "wrangler pages deploy . --project-name stock-heat-landing --branch main --skip-caching",
    "deploy:preview": "wrangler pages deploy . --project-name stock-heat-landing --skip-caching"
  },
  "devDependencies": {
    "wrangler": "^4.123.0"
  }
}
